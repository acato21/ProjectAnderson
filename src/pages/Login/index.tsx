import { FormLog } from "../components/FormLog"

export default function Login(){

    const inputs = [
        {type: "email"}
    ]

    return(
        <div>
            <FormLog  />
        </div>
    )
}